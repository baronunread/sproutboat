# Copy to /etc/sproutboat/caddy.env on the VPS. Never commit a real token.
ACME_EMAIL=admin@example.com
# Scoped to the sproutboat.com zone: Zone:Read and DNS:Edit only.
CLOUDFLARE_API_TOKEN=replace-with-a-scoped-cloudflare-api-token
