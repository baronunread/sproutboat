# Copy to /etc/porffer/caddy.env on the VPS. Never commit a real token.
ACME_EMAIL=operator@example.com
# Scoped to the porffer.dev zone: Zone:Read and DNS:Edit only.
CLOUDFLARE_API_TOKEN=replace-with-a-scoped-cloudflare-api-token
